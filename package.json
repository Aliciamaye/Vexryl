{
  "name": "vexryl-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd apps/web && npm install --legacy-peer-deps && cd ../api && npm install",
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "dev:web": "cd apps/web && npm run dev",
    "dev:api": "cd apps/api && npm run start",
    "build": "cd apps/web && npm install --legacy-peer-deps && npm run build",
    "build:cf": "npm run install:web && npm run build:web",
    "install:web": "cd apps/web && npm install --legacy-peer-deps",
    "build:web": "cd apps/web && npm run build",
    "test": "echo \"Visit http://localhost:3000 for the web app and http://localhost:3001 for the API\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wrangler": "^4.34.0"
  },
  "dependencies": {
    "reactflow": "^11.11.4"
  }
}
